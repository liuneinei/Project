module.exports = {
  // 是否为默认值，如果不是默认值将请求 map api
  is_default:true,
  // 默认定位广东省id
  province_id: 20,
  // 默认定位广东省
  province_name: '广东省',
  // 默认广州市id
  city_id: 213,
  // 默认广州市
  city_name: '广州市',
  // config 存储
  store:null,
  // 用户信息
  userinfo:null
}
