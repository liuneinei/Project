<view class="container">
    <view class="page navbar js_show">
        <view class="page__bd" style="padding-bottom:52rpx;">
        <view class="weui-tab">
          <view class="weui-navbar">
            <view class="weui-navbar__item" bindtap="servertag">
              <view class="weui-navbar__item__lable">选择类别</view>
              <view class="weui-navbar__item__icon">
                <image src="/images/open_up.png" class="navbar__item__i"></image>
              </view>
            </view>
            <view class="navbar__item__border">
              <view class="navbar__item__bi"></view>
            </view>
            <view class="weui-navbar__item weui-bar__item_on" bindtap="citytap">
              <view class="weui-navbar__item__lable">选择地区</view>
              <view class="weui-navbar__item__icon">
                <image src="/images/open_up.png" class="navbar__item__i"></image>
              </view>
            </view>
          </view>
          <view class="weui-tab__panel">
            <view class="geo-container" hidden="{{geoshow}}">
            <block wx:if="{{classshow == 1}}">
                <view class="server-container">
                   <view class="geo-item server-width">分类分类</view>
                   <view class="geo-item server-width">分类分类</view>
                   <view class="geo-item server-width">分类分类</view>
                   <view class="geo-item server-width">分类分类</view>
                </view>
              </block>
              <block wx:if="{{classshow == 2}}">
              <view class="nav-dorpmenu">
                <view class="province-container" style="height:{{wHeight}}px">
                    <view wx:for="{{region}}" wx:for-item="item" class="geo-item province-width {{item.id == regnavCus ? 'checkon':''}}" bindtap="provinceTap" data-id="{{item.id}}">{{item.name}}</view>
                </view>
                <view class="city-container" style="height:{{wHeight}}px">
                  <view  wx:for="{{citys}}" wx:for-item="item" class="geo-item city-width">{{item.name}}</view>
                </view>
                </view>
              </block>
            </view>
          </view>
        </view>
     </view>
    </view>
    <view class="blank20"></view>
    <view class="item-main">
    <scroll-view style="height: 100%;" scroll-y="true" class="container film-list" bindscroll="scroll" bindscrolltolower="scrolltolower">
        <block wx:for-items="{{lecturers}}" wx:for-item="lecturer">
            <view class="main-list" bindtap="bingInfo" data-id="{{lecturer.id}}">
                <view class="module__left module__item">
                    <view class="module__left__img">
                        <image src="/images/icon_tabbar.png" class="module__left__icon"></image>
                    </view>
                </view>
                <view class="module__right module__item">
                    <view class="m_t">
                        <view class="m_t_l">崔老师</view>
                        <view class="m_t_r">分娩导乐</view>
                    </view>
                    <view class="m_b">
                    主治医师;从事儿科临床工作20余年，对儿科常见病、新生儿疾病方面有着丰富经验
                    </view>
                </view>
            </view>
        </block>
    </scroll-view>
    </view>
</view>
   
